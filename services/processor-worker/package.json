{
  "name": "@xray/processor-worker",
  "version": "1.0.0",
  "description": "Processor worker service for X-Ray system",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@xray/shared-types": "*",
    "@clickhouse/client": "^0.2.7",
    "minio": "^7.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}

